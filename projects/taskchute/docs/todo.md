- [x] タスクの入力はセクション欄に直接入力できるようにする
- [ ] 上部フォームを「いますぐ開始」専用に改修する
  - 目的: 計画入力（各セクション末尾の入力欄）とは別系統で、思いついた
    瞬間にタスクを開始できる導線を上部に置く。「あとで何時から何をやる」
    を作るのが下部のセクション入力、「いま始めるものを記録する」のが上部。
  - [ ] ボタン1つで「作成 + 即開始」を一発で行う（内部的には `POST /tasks`
        → `POST /tasks/:id/start` 相当。バックエンドに専用エンドポイント
        `POST /tasks/start_now` を追加して1リクエスト化するのを推奨）
  - [ ] 開始時刻は操作時点の現在時刻で確定。ユーザーが事前に選ぶ余地はない
        （あとから「開始時間を変更」TODO の機能で修正可能）
  - [ ] タイトル未入力でも開始できる（仮タイトル例: `無題のタスク` を保存し、
        TaskRow のインライン編集で後から命名する。空文字保存はモデルの
        `validates :title, presence: true` 制約を満たさないため要対応）
  - [ ] セクションは現在時刻から自動判定する。境界値は要決定:
        - 例: 04:00–10:59 = morning / 11:00–16:59 = noon / それ以外 = night
        - 設定可能にするかは v1 ではスコープ外
  - [ ] 見積分は上部フォームから削除。未設定で作成し、必要なら TaskRow から
        編集する。Task モデルのデフォルト値（現在 0）を確認し、0 のままで
        差分計算が壊れないことを確認する
  - [ ] UI: `app/javascript/components/TaskForm.tsx` を簡素化
        - 残すもの: タイトル入力（任意）/ 「いま開始」ボタン
        - 削除するもの: セクション選択 / 見積分入力 / `+ 追加`ボタン
        - 既に進行中タスクが存在する場合の挙動を決定（自動終了 → 新規開始、
          または開始拒否でメッセージ表示。TaskChute 的には自動終了が自然）
  - [ ] スタイル: `TaskForm.module.css` のグリッドを `1fr auto` 程度に再構成
  - 関連 TODO: 下記「タスク名をいれないで〜」を内包。
    「開始/終了時間を変更〜」はこのフローの「あとから修正」担保として必須
- [ ] タスク名をいれないでもタスクを始められるようにする
  - 上の「いますぐ開始」改修に内包。単独で残す場合はセクション末尾入力欄でも
    タイトル空送信を許容するか別途判断
- [ ] タスクの開始時間を変更できるようにする
  - TaskRow に `started_at` の編集 UI（time input または時刻ピッカー）
  - `PATCH /tasks/:id` の許可パラメータに `started_at` を追加
    （`app/controllers/tasks_controller.rb#task_params`）
- [ ] タスクの終了時間を変更できるようにする
  - TaskRow に `finished_at` の編集 UI
  - `task_params` に `finished_at` を追加
  - `finished_at` を変更すると `Task#actual_minutes` が自動再計算される
    （`started_at` と `finished_at` の差から算出するため追加実装不要）
  - 終了時刻 < 開始時刻 のバリデーションを Task モデルに追加
